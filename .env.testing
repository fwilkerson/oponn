# Testing Environment Defaults
OPONN_ENV=testing
OPONN_SKIP_CSRF=true

# Ensure we fall back to in-memory repos unless testcontainers overrides these
DATABASE_URL=
REDIS_URL=

# Dummy key to allow startup before LocalStack is ready
OPONN_KMS_KEY_ID=test-kek-placeholder
