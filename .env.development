# Development Environment Defaults
OPONN_ENV=development

# Default to In-Memory mode for Zero-Config experience
# These empty values will override any values in the base .env
DATABASE_URL=
REDIS_URL=

# Force LocalMasterKeyProvider by clearing the KMS key ID
OPONN_KMS_KEY_ID=
